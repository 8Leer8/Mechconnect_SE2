/**
 * Shop-owner Map tab uses the same nearby-broadcast map as the mechanic app.
 * `map.tsx` detects `(shopownerTabs)` via pathname for wallet + accept rules.
 */
export { default } from '../../(mechanicTabs)/main/map';
